# @chkp/cloudguard-waf-mcp (deprecated)

This package has been renamed to [`@chkp/checkpoint-waf-mcp`](https://www.npmjs.com/package/@chkp/checkpoint-waf-mcp).

`@chkp/cloudguard-waf-mcp` no longer contains a working MCP server — running it prints a message
pointing to the new package name and exits. Update your MCP client configuration to use
`@chkp/checkpoint-waf-mcp` (bin: `checkpoint-waf-mcp`) instead.

No further updates will be published under this package name.
