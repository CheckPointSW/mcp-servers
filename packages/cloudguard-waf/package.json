{
  "name": "@chkp/cloudguard-waf-mcp",
  "version": "1.0.0",
  "description": "DEPRECATED: renamed to @chkp/checkpoint-waf-mcp. This package is a tombstone and no longer functional.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "cloudguard-waf-mcp": "./dist/index.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/CheckPointSW/mcp-servers.git",
    "directory": "packages/cloudguard-waf"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc && npx shx chmod +x ./dist/index.js",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^5.0.4"
  }
}
